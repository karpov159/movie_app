import './Error.scss';

const Error = () => {
	return (
		<div className='error'>
			<h2 className='error__title'>
				Failed to load the page. Try enabling VPN.
			</h2>
		</div>
	);
};

export default Error;
